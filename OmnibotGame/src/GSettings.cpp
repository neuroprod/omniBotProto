#include "GSettings.h"

float  GSettings::playerRad = 50;
int GSettings::windowWidth = 1280;
int GSettings::windowHeight=720;


int GSettings::tileSize = 256 * 2;
int GSettings::numTiles=10;
int GSettings::worldSize = GSettings::tileSize*GSettings::numTiles;



int GSettings::numLeafs = 500000;
int GSettings::numGrass = 300000;
int GSettings::numFlowers = 500;